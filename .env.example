# URL to the manifest.json file
# Leave empty to use local /manifest.json (default for development)
# Set to your CDN URL when using remote storage (e.g. Cloudflare R2)
# Example: VITE_MANIFEST_URL=https://your-bucket.r2.dev/manifest.json
VITE_MANIFEST_URL=
